import 'package:flutter/material.dart';

class VarticalCategoryList extends StatelessWidget {
  const VarticalCategoryList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
