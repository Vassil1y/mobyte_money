part of 'navigation_bar_bloc.dart';

class NavigationBarState extends StatelessWidget {
  const NavigationBarState({Key? key, required this.child}) : super(key: key);
  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
